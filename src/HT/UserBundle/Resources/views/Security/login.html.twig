{% extends "HTMainBundle::layout.html.twig" %}


{% block body %}

	<!-- header page : banner with page title -->

	<div class="container-fluid">
		<div class="row banner team d-flex align-items-center">
			<h1 class="col-12 animated zoomIn">Bonjour toi !</h1>
		</div>
	</div>

<div class="container">
	
	

	{% if error.message is defined%}
		<div class="alert-warning alert">Votre adresse e-mail et/ou votre mot de passe ne sont pas corrects. </div>
	{% endif %}

	<form action="{{ path('login_check') }}" method="post">

		<div class="form-group">
			<label for="username"> E-mail : </label>
			<input type="text" class="form-control" id="username" name="_username" value="{{ last_username }}"/>
		</div>
		<div class="form-group">
			<label for="password"> Mot de passe : </label><br/>
			<input type="password" class="form-control"  id="password" name="_password"/>
			
		<input type="submit" value="Connexion"/>
		</div>



	</form>

	<a href="{{ path('password_forget') }}">Mot de passe oublié </a>
</div>

{% endblock %}